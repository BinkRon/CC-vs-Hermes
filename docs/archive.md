# 归档 · 已完成记录

> progress.md 的历史沉淀区：已完成章节的一句话结论、历次整改/审计的变更记录、已稳定的架构与约定。
> 运行态看板见 `progress.md`；强制规则见 `contract.md`；稳定共识/计划见 `framework-and-plan.md`。
> 这里的结论均为**回审后的准确版**（若与旧记忆冲突，以此为准）。

---

## 已沉淀 findings（各章一句话结论 · 回审后）

- `01-inside-control-loop.html` —（**2026-07-06 CH-A1R-01 结构手术后的新主线**）**「连续工作」不是模型的能力，是壳用三层防线撑起的一种幻觉**：模型天生一问一答、说完即死，能连干 60 轮靠三层机制栈——**①机械续命**（壳把历史重装后再唤模型，模型的「记忆」＝每轮重喂的历史）；**②提示词纪律**（系统提示词写死「别交桩、干到底」这类反罢工指令，赌模型自觉）；**③壳强制续跑**（错误回喂而非中止、空输出补救、停止钩子、token 预算续跑，代码级兜底）；外加**反面刹车**防停不下来（步数/预算/去重上限）。**核心分野＝上两层的加压逻辑**：两家都「看人下菜」加纪律，但变量不同——**CC 主导轴＝按场景/在场**（终端焦点、自主模式、开了某功能才加压），**Hermes 独有一根轴＝按模型上不上弱名单**（CC 只面对一个托管模型故无此轴）。一句话：**CC 默认赌模型自觉，Hermes 用壳去补模型的不足。** 关键限定（保留自回审）：CC 的停止钩子/token 预算续跑都是 **opt-in 非默认**（token 续跑是「催多干地板」非「防烧钱天花板」）；Hermes 的 verify-before-done 门是 **surface-aware**（编码入口默认开、聊天平台默认关、只对代码改动触发），属场景轴不属模型轴；两家 todo/计划工具都不强制（CC 壳主动软催、Hermes 待办刻意不挂钩子）。
- `02-inside-input.html` —（**2026-07-07 CH-A1R-02 结构手术后的新主线**）**每轮那个 API 请求包是怎么拼出来的——分三层**：①**静态层**（系统提示/工具目录/CLAUDE.md）冻结在最前吃 KV 缓存；②**动态层**每轮把新东西（环境/记忆/上一轮结果）塞进消息窗口；③**压缩应急阀**只在快满时才动。**thesis（方案 A·诚实版）＝装配这件事八成是两家共享的手艺，只有三条缝分叉、且分叉出自两个不同根因**：静态层「按模型分叉」＋压缩层「压缩形态能否训回权重」＝**模型所有权与异质性**（呼应轴一那根「拥有模型」杠杆——Hermes 壳+模型配套造训练数据、意图让模型见过被压历史，"已训进线上权重"非本仓库可证；CC 纯改壳把总结外包给通用模型的提示词）；动态层「注入什么子系统」＝**产品定位 vs 通用底座**之别（另一回事，不硬收进单一根因）。**记忆哲学**（保留旧结论）：CC 当"易过期便签"（用前必验）、Hermes 当"不被摘要冲淡的底稿"；详细生命周期指路 08/05。诚实限定（Codex 采纳 6 修正）：cache_control 非 Claude-only（改分档表述）；压缩标记＝训练管线本身+同源 legacy 标记（`[CONTEXT COMPACTION — REFERENCE ONLY]`/`[CONTEXT SUMMARY]:` 只是 legacy，非"同一个运行时标记"）；CC 待办/任务每 **10** 轮催、计划/自主才 5；压缩"护首尾"非"护首3尾20"。
- `03-inside-output.html` — 运行时**两家都读结构化工具调用**（都不解析散文），真正分野是**壳兜底分两层**：**下层协议地板两家共享**（调用↔结果配对补全防悬空崩请求、只读并发/写串行分区、参数校验回喂、落盘指路、连 stop_reason 都不信而数 tool_use 块）；**上层弱模型纠偏只有 Hermes 铺**（工具名拼错→模糊改名 / 空名→"防鹦鹉" / 剥掉正文里的假工具调用），CC 故意留空——工具名对不上只合成 `<tool_use_error>` 回喂让模型自纠，赌托管的强模型能自改。`<tool_call>`/`<tool_response>` XML 是**训练/轨迹格式**，非运行时格式。工具目录都用"常驻+按需拉取"防撑爆（触发默认略不同：CC 默认对 MCP/可延迟工具总是延迟，10% 只是其 auto 模式阈值；Hermes 默认按 10% 阈值）。
  - **整改（2026-07-05，PM 复审驱动）**：原稿把 CC 这层写成"壳极薄/几乎不设防"——**以偏概全**。经 CC 代码核验（`Tool.ts:358`、`toolExecution.ts:369-411/614-680`、`query.ts:123-149`、`toolOrchestration.ts:91-116`；全仓搜 fuzzy/近似匹配零命中）：CC **有健全的协议级兜底**，缺的只是**面向弱模型的语义纠偏**（模糊改名/防鹦鹉/剥假调用/参数自修复/去重）——薄的不是兜底本身，是这一层。故事从粗糙的"薄 vs 厚"改为**"同一层协议地板 + Hermes 多铺一层弱模型纠偏"**。三处落地：§02 CC 流程图补回缺失的"查无/参数错→报错自纠"分支并语义配色（判断=amber、Hermes 差异节点"模糊改名"=紫高亮）；§04 判词 A 去掉"壳几乎不设防"；§03 hero 由**通用六步 stepper**（形式主义、未署名、画的其实是 CC 流程）换成扛论点的 bespoke **两层网重档图**（`.netfig` scoped）。「世界/环境」经查证：改名只限第六章关系名（部署环境），本章"世界"＝工具改动的真实对象，保留。
- `SYN-inside-story.html` —（综合①·轴一收尾 / **2026-07-07 CH-A1R-04 结构手术后**）**一个回合的一生端到端 ＋ 设计理念收束**：上半用回合时间线走通「模型说完即死→壳机械续命→逐块处置→错误回喂→再唤模型」8 站闭环；下半把轴一三章收束为 **thesis＝装配与运行八成是两家共享的手艺，只有两条杠杆在分叉**——**第一杠杆＝信任观**（CC 默认赌托管强模型自觉→壳做薄，Hermes 用壳补模型不足→壳做厚）；**第二杠杆＝谁拥有模型**（Hermes 壳＋模型配套、可把壳内形态训回权重；CC 纯改壳、权重级变强外包下一代基座）；**外加一条诚实例外**：动态层「注入什么子系统」＝产品定位 vs 通用底座之别，不硬收进两杠杆。分叉汇流图＝8 处分叉归因（改壳 6／改模型 1／产品定位例外 1）。**「选择互相锁定」**：信任观决定壳的厚薄、所有权决定第二杠杆能否存在，两者非独立选项。
- `04-outside-time-trigger.html` —（轴二·时间与触发）表面两家趋同（都有 cron/后台/远程触发/推送），**真正分水岭是"谁替 agent 保活"**：CC 厂商托管→靠厂商云/平台保活、假设"人会回到会话"、本地进程有意不做常驻守护、真·无人长跑外包给厂商云（Teleport）；Hermes 自托管开源→没人保活、被逼**自己变成常驻实体**（自带网关长驻、被时钟/webhook/消息叫醒、跑完主动推给不在场的人）。**所有权对象从"谁拥有模型"（轴一）换成"谁拥有部署"（轴二）**。反直觉：做"最小开源壳"的一方在"独立跨时间存在"上反而做得最重。回审修正：scale-to-zero 自主休眠是 **Phase-0 实验、默认关、需 NAS Labs 开关+Fly.io**（非默认支柱）；持久化无 exactly-once；CC/Hermes 非绝对二分（CC 也有无头/SDK 常驻）。**FIX-02 补强（2026-07-03）**：矩阵加「单会话内·一条长命令」一行（同步阻塞 vs 后台并发），把原只在页脚的机制提进正文。**回审推翻初稿的"CC=命令级 vs Hermes=子代理级"二分**（Codex completeness 抓到，正是"为干净故事以偏概全"的病根）：实为**两家都覆盖"一条命令"和"一整个子代理/批次"两种粒度**，真差别是 **CC 自动解绑（阻塞命令满 15 秒自动转后台）vs Hermes 显式声明（前台等太久被拒、劝改后台）**。
- `05-outside-programmability.html` —（轴二·可编程性）两家都把 harness 做成高度可编程平台，但**开放的是两种东西、信任治理归属相反**：CC 闭源托管→用户碰不到核心，官方只能**凿固定插槽**，且因扩展在陌生人间分发+企业多租户，自建**市场+企业只读总闸**式治理；Hermes 开源自托管→握着源码，**几乎一切做成可替换 provider** + 改源码，社区扩展走"零核心改动"插件，**不必建企业市场式治理墙**（靠 opt-in/显式授权/安全扫描等轻边界）。**所有权链条第三环："谁拥有代码"**（轴一模型→ch4部署→ch5代码）。回审修正：CC 换模型锁的只是**线协议/推理内核**（可经 `ANTHROPIC_BASE_URL` 接任意网关跑第三方/开源模型），非"不能接第三方"；Hermes 有 pip/PyPI 陌生人分发通道；钩子枚举其实两家对称（真差别是"能否改源码新增钩子"）。
- `06-outside-environment.html` —（轴二·环境）两家在「动作落在哪、闯祸谁兜底」上**惊人趋同**：默认都不关沙箱（CC 命令沙箱默认关、Hermes 后端默认 local＝裸主机，动作直落真机器）、都把「隔离」与「审批」当**替代**关系（关进笼子就不再逐条问）、都有一道连「全速前进」都压不过的**绝对地板**、被拒时都给模型**近乎逐字重合**的「别耍花招绕过」训诫（我们独有的并排王牌）。真正分野＝**「在场契约」**：CC 厂商托管、原生假设人在场（弹窗＋Shift+Tab 旋钮），无人化靠预授权把点击提前签好、没人回答默认拒；Hermes 开源自托管、常态被消息叫醒无人值守，必须为「没人点确认」定义确定行为（cron 默认拒／沉默≠同意／硬禁地板永在），且因无厂商兜底而**极度诚实**（白纸黑字「进程内没有任何东西算隔离，唯一边界是 OS」，连凭据剥离都自认「不是隔离、只减少随手泄露」）。**Codex 门推翻深潜一初稿的「两道地板护不同东西」过满写法**（正是以偏概全病根）：两家其实都两样都护，真差别是**各自把对方留作软拦的那样单独升格成硬地板**——CC 硬护「管着 agent 自己的配置层」(.git/.claude/shell/IDE)、Hermes 硬护「别毁机器」(删根/格盘/关机)。诚实点全部保留且**不埋脚注**：两家沙箱/隔离都是默认关的 opt-in；Hermes 有「非交互非网关非 cron 无人会话→**直接放行·不拦不问**」的诚实漏洞（经 Codex 修正比初稿「仅告警」更严重，已进**主流程图第三支**）；Tirith 默认 fail-open；CC auto 模式 ant-only；子代理默认不各自隔离工作树。verdict 显式回答在场问题→喂综合②在场契约脊椎。**learn-cc s03 权限已随此章收**（五模式/顺序检查链/bypass 硬例外/危险模式剥离/PreToolUse 钩子）。
- `07-outside-multiagent.html` —（轴二·多智能体）两家**底座一样**（模型自主 fan-out 隔离分身）**且都能盯/打断单个分身**（CC 分屏面板、Hermes TUI /agents 面板）——分野**不在"能不能监督"**。真正分野是**协调隐喻**：CC 把分身编成"**互相发消息的同事团队**"（具名队友/邮箱点对点+广播/共享看板/协调者，人当队长在同一消息网），live-team 产品面**更完整更产品化**；Hermes 日常是自主 fan-out（委派分身不对话），重活走"**靠共享数据库状态协调的看板/蜂群流水线**"（角色=独立实例、依赖图/黑板/原子认领），需显式进入。ch-04"在场"联系=**合理解读非代码可证**。反直觉：把"互相协作的 AI 同事团队"做最完整的是**托管产品 CC**。**经两轮返工**：① 用户指出 subagent≠team；② 回审指出 **fork≠subagent**（fork 继承上下文、复用缓存、CC 独有，Hermes 委派只有 fresh）、Hermes 非黑盒、团队非默认关闭、CC 计划审批自动通过。CC 委派实为**三分身**：subagent（全新隔离）/ fork（继承上下文）/ 团队。
- `08-outside-self-improvement.html` —（轴二·对自己/学习闭环）**这一侧是轴一「谁拥有模型」杠杆在轴二的回归**。两家可证的「变强」**形态上趋同**——都在**壳里**（把经验蒸馏成跨会话记忆，不改权重）、都**刻意不盲信**记忆（用前先验）。真正分野是**结构潜力**：CC 不拥有模型→结构上**不可能**把学习闭进权重、只能把权重级变强**外包给下一代基座**（仓库外·不可证）；Hermes 拥有模型→**握着**把壳内轨迹喂回训练的杠杆、仓库里真有产训练数据的管线（可证）。**核心对称诚实（两门都确认公允）**：两家仓库**都证不到「你的使用让它权重级变强」**——CC 断在「没有那个能力」，Hermes 断在「闭环没在仓库合上、且产数据管线吃研究员策划集、不接你的活会话（`batch_runner` 明写 skip_memory）」。所以「self-improving/越用越强」可证的都是壳内记忆、不可证的都得**对称标「意图」**。**两处最有产品洞察**：① 变强＝**分时间尺度整理记忆**（回合内→回合末→跨天做梦/空闲策展→跨人团队）；② **自我改进也会自我污染**（Hermes 出过「存错假设」事故→黑名单+审批闸；CC 用「召回纪律」预防）——差别在这一课**在实验室学会（CC eval 硬化）还是生产被投诉后学会（Hermes）**。**真分野**：Hermes 后台复盘**自主新建/改进技能**（默认，强偏好改现有、新建兜底），CC **系统从不自主发明新技能**（造技能全人触发+多内部限定）；且论后台自我改进的默认力度，**Hermes 反比 CC 更「开」**（回审修正了初稿"两家都把深度整理藏门后"的反向以偏概全）。verdict 双仪器＋显式答在场问题→喂综合②脊椎。

- `SYN-outside-story.html` —（综合②·轴二收尾）**五种关系底下是同一个 master variable：「这个 agent 假设有没有人正盯着」。** CC 处处落「有人在」（人会回到会话／原生弹窗＋旋钮为人点设计／人当队长在消息网／变强要你签字上传转录），Hermes 处处落「没人在」（自己变常驻实体／cron 默认拒·沉默≠同意／数据库状态协调不需人当协调者／产数据管线绕开你的活会话）。**在场契约不是自由选择，是所有权链逼出来的**：谁拥有模型（轴一）→部署（04）→代码（05）→有没有厂商兜底→敢不敢假设有人在。反直觉：做「最小开源壳」的 Hermes，反因没人替它在场，只能把「独立存在」做到最重。**诚实纪律（写进主视觉，非脚注）**：这根脊**不是五等份**——04/06/07 是在场契约主导（实心点），05/08 实为所有权链在分、在场只是侧面（空心点），07「人当队长 vs 数据库协调」是合理解读非代码可证（半点）；且明写 08 的对称诚实「两家都证不到你的使用让它权重级变强」；ACP 外来 agent 互通仍待查证（framework 复审点未闭，见下）。

---

## 里程碑与整改记录

### DSN-03 设计宪法按「控制机制」重排为 Part 0–3（2026-07-08，PM 驱动）
- **动因**：PM 质疑 `design-principles.md` 里 `中档 ≤2`、`bespoke ≤1` 等配额"不从第一性原理出发、是拍的"。诊断更精确：**不是内容太多，是四类东西（原则/规则/工序/手法库）搅在一起、按"主题"而非"决策时刻"排列，且一条规则住两处（§5 门复述 §1–2 规则）→ 漂移**。文件本身已是补丁沉积（§0 删了重档上限、§3 的 `≤1` 还在；contract §33 还写"重档 1 个"与 §0 矛盾）。
- **根隐喻（新写进文件抬头）**：一份设计宪法 = 跑在"目标不可完全编码＋执行者（含作者）有已知偏差＋验收昂贵滞后"上的**控制机制**，每条规则为压低四类损失（规格误差／被偏差带偏／检查成本／打补丁漂移）而存在。推论：**规则尽量管结果不管计数；每条标"明线"（判断不可信处硬卡）还是"判断"，并带"防哪个翻车／何时破例"。**
- **改法**：重排为 **Part 0 一页纸原则 / Part 1 规则表（每条只住一处，判断规则带四列） / Part 2 工序＋设计门（门＝过 Part 1 全表，不复述） / Part 3 附录（手法库＋先例，查阅不线性读）**。核心规则替换：**删 `中档≤2`／`重档 1 个`／`bespoke≤1` 硬卡 → 换"梯度而非数量"**（视觉脚本给全章块排全序＋论证第 1 名＋点名安静基线；门查排名与基线不数个数）；保留偏差护栏为明线（`粗体≤2/段`、`裸文vs框`、`不引入新品牌色`）；修 §0/§3 矛盾（bespoke「≤1 处」明确为**代码块数、非重档个数**）。
- **连带对齐**：`contract.md` 五处（两层制 §1E／流程图 §1B／视觉重量宪法→梯度／视觉脚本 Part 2.1／设计门 Part 2.2）＋ `CLAUDE.md` 设计门条目，全部改指新 Part 号、去"三档配额"旧词。旧 § 号引用只留在本 archive 历史条目里（历史记录不回改）。`_design-system.html`、`build.py` 未动（tokens/组件/bespoke 抽取机制不变）。
- **review 一轮（同日，5 修）**：外部 review 用本文件自己的框架反查，抓到重排中**两处静默丢失**（讽刺：正是在重写"谁都不许静默消失"的文件时犯的，坐实了"靠独立完整性门、不靠下次更仔细"）——① 旧 §6.4「卡内只放标签级短语、解释放卡外裸文短段」丢失 → 补回 §3A（字数单位统一为"渲染后 ≤4 行"）；② 设计门把"抽查采样"（任抽 3 段粗体/3 标题）当规则复述删掉，实为审计方法 → 门恢复"抽查"项、并把主体改为"逐条按 Part 1 过火信号列自检"（防橡皮图章，兼收「降档优先」明线的可执行性靠信号列这一张力）。另 3 修：③ 梯度自查加"安静基线须占全章大部分块"（把规则的"大部分块"从口号变可查，堵中档平均用力）；④ `contract.md:24` stepper `1–3` 计数配额加定性半句"此为交互/工程配额非视觉重量配额、勿拿梯度非数量自我豁免"（计数在边际成本均匀时是好代理，与视觉重量不同）；⑤ 「词条可解码」从机械规则改归判断规则（判断哪个词算黑话需判断）。
- **PM 反馈驱动的设计转向**：PM 指出全项目"可视化与排版有点形式主义"（对标 learn-claude-code：hero/对比按内容设计，简单的简单画、复杂的精心设计，字排更考究）。诊断出四处病灶：① 综合① §05 stepper＝§02 矩阵的动画重播（纯格式预算）；② 把"单机制 finding 骨架"套到综合头上，被迫制造"问题故事/深潜"；③ 全站一个视觉重量（thesis 与边角 caveat 一样"盒子感"）；④ 字排单调（通篇 1.7 行距、只有 h1/h2/h3）。**PM 拍板"收官章配得上特别对待"** → 综合②内容优先重做、引入本章专属排版当"新排版标杆"（前 9 章是否回头提升，PM 看完再定）。
- **内容优先的形式选择**：不套固定骨架、**不用 mermaid、不用 stepper**——手工搭两张图：**「在场之脊」**（一条脊＝在场总变量，五关系横跨、CC 全落左/Hermes 全落右，圆点分级实心/半/空心把"不是五等份"的诚实做进主视觉）＋**「所有权链」**（模型→部署→代码→兜底→敢不敢假设有人在的纵向阶梯）。排版：大字引文式 thesis、编辑式 kicker+大标题、去描边横线的机械感、减少盒套盒。副作用红利：无 mermaid＝无离线白屏风险、无 stepper＝无死按钮风险。
- **build.py 机制升级（新增·可复用）**：综合②需本章专属 CSS，但 `extract_body` 只搬 `.wrap` 内正文、单章 `<style>` 不进合订本（否则单章好看、合订本静默掉样式）。故加 `extract_bespoke_css()`：收集各章 `<style class="bespoke">`（须 scoped 在本章根 class 如 `.syn2` 下，防泄漏别章）并入合订本 head。共享设计系统仍单一真相，bespoke 是显式声明的定制层。**此机制通用**：以后任何"定制章"都走这条。
- **门记录（综合篇的门做了裁剪，如实记录）**：① 自审过。② **忠实度门（综合篇最关键的一道）**：独立 agent 对照五章原稿逐条核"编织是否忠实/有无为对称以偏概全"，**无结论级必修**；采纳 2 润色——补 08 招牌对称诚实句（我 row5 原只隐含传达，是"为干净故事丢限定"的老病根，必补）、row4「盯着」→「把关」去凝视感。③ **Codex soundness 有意不重跑**：综合"只准编织不准开采"、不产生新代码级断言，忠实度门"特别追问 D"已独立确认那条所有权→在场因果链源自 04/06 判词、非新造；重验已过门事实属低价值——这是**我形成的判断**（CLAUDE.md：门不机械跑），如实记录不假装。
- **framework 复审点（部分闭合）**：framework-and-plan.md 立的"五关系完备性在 06/08 完成+ACP 查证后复审"——06/08 材料已证五关系成立、05/08 那两格"实为所有权"已在综合②诚实标注（未硬掰成在场）；**唯 ACP 外来 agent 互通仍未查证**（07 未覆盖），综合②在悬而未决明写"若那格成立，五关系边界要回来再调"，不因故事顺盖章。此复审点**仍留一条开口**，不算完全闭合。
- **验收**：单章内联脚本 node --check OK（脚本为设计系统骨架、本页无 mermaid/stepper 实体）；playwright 经本地 http server smoke——合订本 0 console error、bespoke CSS 确进 head 并生效（琥珀实心点/脊柱竖线/引文宽度/5 srow/5 chain 全命中）、chapter 正常切换。合订本 **10 章 · 两轴 + 两综合全线完成**。
- **出稿后 PM 抓到排版 bug + 外科修复（2026-07-04）**：PM 指出"内容宽度忽宽忽窄、导读字大于正文、字体层次不统一"。**根因**：每个正文块用 `em`（相对各自字号）单独设 max-width，字号不同→落地像素宽不同（引文 33em@22px=726、正文 39em@16.5px=643、链 44em=704、标题/主图无上限=满宽），七八个右边缘=毛边；"导读大于正文"是引文 22px 既大又宽压过正文。**修法**：立一条**不随字号缩放的统一阅读栏 `--col:680px`**，**全章从上到下同一个宽度**——正文块 + 标题 + 主图「在场之脊」+ 所有权链 **全部共用**（实测右缘全对齐 972/左缘 292，六块一条边）；字阶轻收（h1 41→37、lede 22→20、h2 27→24）。**踩过的坑（记下防复犯）**：第一版我自作聪明给主图单开 `--fig:60rem` 当"图破栏"（以为正文定宽+图破栏是加分），被 PM 二次打回——一张宽图杵在窄正文和窄所有权链（同是框图却窄）中间，"一个图宽一个图窄"本身就是不一致；**收官章此刻要的是一致、不是花活**，已取消破栏、主图同收 `--col`（中柱 148→116、单元格 padding/字号微调防挤）。**给 DSN-01 的硬规则**：① 正文宽度用**字号无关单位的单一 `--col`**，禁止每块随手拍 `em`；② 破栏/图更宽这类"编辑手法"要克制——除非全书成体系，否则单章里一处破栏＝制造不一致。这正是"模板填空/自作聪明加花"在排版层的两种翻版。

### CH-A1R-01 轴一手术批一·01 章 Loop 改写（2026-07-06，PM 验收通过，兼 DSN-02 排版 pilot）
- **背景**：PM 诊断轴一四篇「细节清楚、骨架缺失」——被「每章一个 hero 深潜」体例带偏，缺全局地图。批一把 01 章主线从旧版「谁握终止权（收工哲学二分）」换成 PM 真正要的认知台阶：**一问一答的模型为什么能连干 60 轮**（反直觉），答案＝三层防罢工机制栈（机械续命/提示词纪律/壳强制续跑）+ 反面刹车。旧「信任框架」从主线降为判词保留（verdict 处）。**总原则：框架重排 + 定向补缺，不推倒重写。**
- **素材**：两库各 1 研究员（Opus max）产结构化 JSON（layers L1–L4 + misfits，每机制带 file:line + verbatim 制品），我收敛成三层论点。核心升级：研究揭示两家都分层加纪律，但**分化变量不同**——CC 按场景/在场，Hermes 多一根「按模型上弱名单」轴（因 CC 只面对一个模型）。框成「主导轴/独有轴」而非绝对二分，且为轴二「在场」脊椎埋线。
- **视觉脚本**：重档 2（全景三层防线地图 bespoke + 继承的 verify 门 stepper）、中档 2（flow2 双 mermaid「幻觉」对照 + 刹车矩阵）、轻档裸文。全部按 design-principles v2 出稿，兼任 DSN-02 排版 pilot。
- **三道门**：自审（3 小修）/ Codex soundness（18 条：13 TRUE、5 PARTIALLY 采纳 3——待办回执系旧版工具、预算退款不只 execute_code、推理超时非统一 600s；2 驳回因成稿未做该断言）/ 独立完整性门（2 必修已修：**编码岗纪律错挂「模型弱名单」桶**实为编码姿态触发、**CC「人不在场」桶混入特性触发项**——两处「显要处绝对二分」统一降为「主导轴/独有轴」口径，正是防系统性以偏概全的活标本；建议 3 条采纳）。机械两关过。
- **PM 两轮反馈**：① 全景图 36 个词条黑话不可解码 → 全部加 hover 一句话解释（`data-tip` + 纯 CSS tooltip，离线可用）；② 开篇层次感差（大字 lede + 裸文挤成一坨）→ 判词回 thesis 绿框、幻觉叙事进 story 虚线框、框内分段（恢复旧版「两框锚点」结构 + 新版分段纪律）。两条沉淀为**模板规则**（design-principles §1.3 开篇两框裁定 + §5「词条可解码」设计门项），批二/三/四直接继承。PM smoke 验收通过。
- **DSN-02 一并了结**：01 手术稿兼排版 pilot，v2 排版纪律经此稿实战验收；旧版存 `assets/01-inside-control-loop.v1-backup-20260706.html`、v2 pilot 存 `assets/01-inside-control-loop.v2-pilot-archived.html`。

### CH-A1R-02 轴一手术批二·02 章输入侧改写（2026-07-07，PM 拍板 done）
- **背景**：接批一「先地图后深潜」思路，把 02 章主线从旧版「上下文满了怎么办」升级为**「每轮那个请求包怎么拼、分三层」**——静态冻结吃缓存／动态每轮注入／压缩应急阀；旧稿的压缩解剖降为第③层一节（阶梯图 `.cmpl` 与 stepper 复用旧稿、按新事实修正），记忆降为动态层注入清单一行、生命周期指路 08/05。
- **thesis（PM 拍板方案 A·诚实版）**：装配八成是共享手艺，只有三条缝分叉且出自**两个不同根因**——静态/压缩＝模型所有权与异质性（呼应轴一）、动态＝产品 vs 底座（另一回事，拒绝硬收进单一根因）。**PM 明示动态层对比结论不预设，研究员须客观穷举再分析。**
- **视觉脚本**：重档 2（§01 请求包三层骨架 stack 带 hover 词条＋§04 第二根杠杆真实制品）；中档 3（压缩阶梯 cmpl／注入清单对照／模型分叉 chips）；动态层 44 vs 8 单开红边 guard 防以偏概全。继承批一模板（开篇 thesis+story 两框分段、重档图词条 hover）。
- **三道门**：自审／Codex soundness（18 claim，多条 TRUE、6 PARTIALLY/1 FALSE，**采纳 6 修正**：HM-3 cache_control 分档、HM-6 压缩标记＝训练管线+同源 legacy（**重档 #2 立论订正**）、CC-4 待办每 10 轮、CC-3 "绝大多数 system-reminder"、CC-6 摘要模板补 9 段、HM-5 "护首尾"；未采纳均已 hedged 或归他章）／独立完整性门（**无结论级必修**，四高危论断——44vs8/压缩③级/静态按模型分叉/记忆——全过真实代码，仅 1 处非结论级页脚已修）。
- **PM smoke 设计修正 + DSN 规范回填（系统性缺口而非个案）**：揪出 §04 压缩阶梯下「展开完整对照」入口设计不当（浮在版面缝里的带框按钮＝孤儿）。按「页面先行当样板→PM 验像素→回填规范」走完一轮：① 改 02 一处样板（入口改为紧贴③行正下方、缩进对齐两列的档三文字链，去「9 vs 13」诱饵改中性收据文案，载荷留弹窗）；② 回填规范——`design-principles.md` §1.5 详情入口与弹窗（论点 vs 收据准入／集合型·证据型两形态／相邻免标识·远置需锚定／弹窗 anatomy+反诱饵）、§5 设计门加「详情入口合规」四问、§6.2 补「可点性靠语境不靠边框」反讽；`_design-system.html` 加 `.entry-note` 证据型标准（从 02 `.receipts` 提炼）、`.more` 标为集合型先例、`.detail-btn` 标废保留。
- **旧稿 pre-surgery 整改史**：2026-07-05 那轮 learn-cc 交叉核对+源码定谳（snip/microcompact 次序、keepRecent:5、AUTOCOMPACT_BUFFER_TOKENS 等）针对的是被本次手术替换掉的旧章结构，细节留 git 历史，durable 结论已并入上方新一句话结论。

### CH-A1R-04 轴一手术批四·综合①重写（2026-07-07，PM smoke 通过 done · 轴一手术四批全闭环）
- **背景**：批四把综合①从旧版（§05 stepper＝§02 矩阵的动画重播、形式主义收尾）重写为**上下两半**：上半＝一个回合的一生端到端时间线（吸收已撤销并入的独立需求 EXT-02）、下半＝设计理念对比（保留深化双杠杆论点 ＋「选择互相锁定」论证）。**轴一手术四批（01 Loop／02 输入／03 输出／04 综合①）至此全闭环。**
- **thesis**：八成共享手艺 ＋ 两条分叉杠杆（信任观→壳厚薄、所有权→第二杠杆能否存在）＋ 一条诚实例外（动态层注入＝产品 vs 底座，不硬收进两杠杆）。详见上方一句话结论。
- **视觉脚本**：重档 ×2＝回合时间线 stepper（8 站、⑧→② 回边吸收 PM 草图）＋ 分叉汇流图（8 分叉→改壳 6／改模型 1／例外 1）。
- **门**：机械两关（node --check ＋ 浏览器 smoke 真点 stepper）＋ 四道门全过——自审 4 修／独立完整性门 1 必修 3 可选已修／设计门过／Codex soundness 按综合②先例轻量（综合只编织不开采、无新代码级断言，有意不重跑）。
- **PM 反馈已修（两轮）**：分叉条目全补主语（CC 蓝／Hermes 紫）、药丸改竖排、术语「缝」全书统一改「分叉」（PM 可否决换 gap）。旧版存 `assets/SYN-inside-story.v1-backup-20260707.html`。

### CH-05R 撤销 · 并入 CH-A2R（2026-07-07，PM 裁定）
- **撤销原因**：CH-05R（Loop 与用户扩双向 + Ch08 收窄）自 2026-07-05 领取后停在 `researching`，两章成稿（Ch05 双向章 / Ch08 收窄）始终未渲染。其后 PM 新立轴二整改 campaign `CH-A2R`（产品观脊 + 去八股 + 重排读序），用户章本就是新读序第 2 位、需套 `CH-A2R-01` pilot 样板渲染——CH-05R 的内容职责与 campaign 完全重叠。故 PM 裁定撤销 CH-05R，职责并入**新建的 `CH-A2R-05`（用户章）**。
- **保留的方案资产**（转由 CH-A2R-05 承接，不丢）：thesis＝「差异在控制、共识在适应」；结构决策＝沿「记忆↔飞轮」缝切开，适应半（记忆/教训/用户画像/技能自造/自我污染）迁入 Ch05 下半成双向章（上半 人→Loop 编程 / 下半 Loop→人 适应），飞轮+所有权半留 Ch08 收窄；**Ch08 收窄的结构工作已完成**（08 现为训练 only、适应半已迁 05 下）；防双写火墙表已敲定。
- **（2026-07-07 后续更新，以此为准）**：PM 与讨论 session 定稿 CH-A2R-05 新脉络后，上述两项资产被**升级替代**——thesis「差异在控制、共识在适应」降为素材（新脊＝让渡→「懂用户」内生需求→定义四维→两条路手段→懂≠顺闸→定位 verdict）；「上半/下半双向章」拼接结构**废弃**，改一体化五节（形随内容）。防双写缝微调：技能自造归 05（PM 决策 C）、飞轮+权重级变强留 08、边界执行闸归 06。现行全案以 `backlog.csv` CH-A2R-05 行为准。
- **连带调整**：`SYN2-R` 依赖 `CH-05R+CH-A2R-01~04` → `CH-A2R-01~05`；`MEM-09`「前提被记忆整块迁 Ch05 消解」的承接卡改指 `CH-A2R-05`。

### 完整性回审（2026-07-02）
审计门（契约 7-8）抓到一个**系统性病根**：把"默认行为"用绝对语气写成"全局特征"，而这些默认都有 opt-in/条件化例外（与 ch-07 subagent 事故同族，Codex-soundness 查不出）。**7 条 conclusion-changing 发现全部采纳并修，无误报**：
- **07（HIGH）**：修 subagent/team 时又把 **fork 误并进 subagent** → 改为三分身；fork 归 CC 独有。
- **01 & SYN（中）**：CC "壳从不判断完成"绝对化 → 补停止钩子 + token 预算续跑两个 opt-in 覆盖口，纠正 token 预算"地板非天花板"。
- **SYN（中）**：Hermes verify 门 surface-aware（聊天默认关）→ 加限定；"训进权重"恢复 hedge。
- **04（中）**：scale-to-zero 降级为"实验 opt-in"；删"本地文件变化"误列。
- **05（中×2）**："CC 不能接第三方"改准（ANTHROPIC_BASE_URL）；补 pip 陌生人分发；钩子枚举对称。
- 一批 low（02/03/05）顺手改。验收：7 篇 node --check 全过、合订本重建 7 章、浏览器单章 0 error。

### CH-08「Loop 与自己」出稿 + 三道门（2026-07-04）
- **流水线**：双库研究员（Opus max，各啃一库，一次到位无中断）→ 我收敛（收敛前专门过「以偏概全/夸大飞轮」自查）→ 我亲手渲染。核心机制=**壳/权重墙** `.flow2`（把「变强」劈成可证的改壳 vs 不可证的改权重）；深潜一=**记忆蒸馏时间尺度** stepper（回合内→回合末→跨天/空闲→跨人）；深潜二=**自我污染** `.compare` 真实制品并排（CC 召回纪律 vs Hermes 存错假设事故补救）。
- **门 2 Codex**：8+9 claim 核。多条 PARTIALLY-TRUE 已修：HM8「不接活会话」加边界（有个**默认关**的 `--save_trajectories` 能手动导出当前对话、但不自动训练不接批量管线）；「高度趋同」收窄为「形态上趋同」（Hermes 壳内学习其实更强）；「预防 vs 事后」别写满（Hermes 也有写入前扫描等预防）。HM8 核心刀法（`skip_memory=True`、grep 无会话导入）**强确认**。
- **门 3 独立完整性门**：抓到**1 处方向性反向以偏概全**——初稿 verdict「趁你不在深度整理很多是门后的」对两家一起下，但**对 Hermes 失准**（Hermes 后台复盘 fork 默认开、比 CC 更「开」）。这正是"为对称而对称"的反向病根，已改为非对称。其余为措辞精修。
- **关键方法论**：本章最敏感（飞轮易夸大）。两门**都独立确认**「对称诚实」是挣来的精确划界、非虚假平衡；HM8 那一刀两门都判强成立。收敛时守死的诚实纪律（飞轮从中间切开、对称标意图不可证）全程未破。
- **验收**：node --check + captions JSON 4 帧合法；playwright 单章+合订本 smoke（0 error、真点 stepper 前进到 2/4 帧、mermaid 深色）。合订本 **9 章 · 轴二五篇正文全齐**。**learn-cc s09 记忆**大半随此章收（MEM-09 待按残余缺口评估）。**标题「Loop 与自己」待 PM 定**。

### CH-06「Loop 与环境」出稿 + 三道门（2026-07-03）
- **流水线**：双库研究员（Opus max，各啃一库，含一次 mid-stream API 中断重启）→ 我收敛论点（先逐条过「以偏概全」自查）→ 我亲手渲染单章（无 stepper——机制形状是闸门链+文案并排+矩阵，stepper 不匹配）。
- **门 2 Codex soundness+completeness**：10+10 claim 逐条核。多条 **PARTIALLY-TRUE 已修**：CC「工作区内写基本自由」纠正为「默认仍问、Accept edits 才免问」；「安全的拒和问永远排放行前」收窄为「只有 deny＋自我治理硬核真短路」；子代理 worktree 隔离精确为「可显式开 isolation:worktree、非默认」；**HM 诚实漏洞比初稿更严重**——当前主路径是「直接放行·不先做危险检测」，非「仅告警」（已改主图与悬而未决）。
- **门 3 独立完整性门**：抓到 1 条「应修」——**诚实漏洞只在脚注、主流程图给反向结论**（Hermes 图把「无人」塌缩成 cron→拒），已给主图补第三支「无人·未识别→⚠直接放行」，让丑机制上主视觉。采纳增补 env_passthrough 凭据剥离（论点最好实证）、CC 内部暂存目录窄例外；**驳回**对外渠道白名单（归 04/07、本章无关）。
- **关键方法论**：门 2 与门 3 在深潜一「两道地板」上**给了相反建议**（完整性 agent 说「可写更锋利」、Codex 说「写得过满」）。按 CLAUDE.md 不选边、独立判：采 Codex，把「护不同东西」改成「各自把对方留作软拦的那样升格成硬地板」——既准确又更锋利，恰好躲开「子情形当全部」病根。
- **验收**：node --check OK；playwright 经本地 http server（127.0.0.1:8791）跑通单章+合订本 smoke（0 error/warning、mermaid 深色 SVG、三支流程图正常）——**解决了上 session「受控 Chrome 够不到 localhost」的老限制**（playwright 自带浏览器与本进程同网络命名空间，file:// 仍被拦、走 http 即可）。合订本 8 章。

### 边角债回改 FIX-01 / FIX-02 / FIX-03（2026-07-03）
- **FIX-01 标题统一句式**：04/05/07 的 h1 → 「Loop 与时间 / 用户 / 同类」，副标题 → 题眼问句，机制名降级为 tag（保检索），`<title>` + build.py 侧栏 label 同步。原产品判断句仍完整存于各章 `.thesis` 块，未丢论点。纯表头文本、未碰 `<script>`。（05 标题原作「Loop 与主人」，2026-07-03 按 PM 要求改「用户」——"主人"有奴役感；04 正文同批"主人"叙事一并改"用户"。）
  - **标题再调（2026-07-03，PM 要求"太抽象"）**：**06「Loop 与世界」→「Loop 与环境」**（关系名 对世界→对环境 同步）；**07「Loop 与同类」→「Loop 与其他 Agent」**（分析关系名保留「④ 对同类」不变，只改 reader-facing 标题）。h1/`<title>`/build.py label/framework 章标题列/progress/handoff/backlog 全同步。**08「Loop 与自己」暂不定名**——PM 说等成稿再看。故 archive 与旧记忆冲突以此为准：现行标题是 06 环境 / 07 其他 Agent。
- **FIX-02 04 后台并发补强**：见上方 04 一句话结论的补注。过 Codex soundness（A1/A2/A3/A5、B1/B2 属实；A4 表述过宽已收窄为"疑似等交互输入才提醒"；B3 二分被推翻已重写）。
- **FIX-03 01 计划清单维度**：01 §04「对比」从三维扩到四维，加 **④ 过程纪律：计划清单谁来盯**；07 悬而未决加一条半句指路（单 agent 自列计划归轴一控制侧、非多智能体）。**"对照物须查证"结论——推翻 learn-cc 的"Hermes 待办钩子"假设**：Hermes 的 `todo` 工具**刻意不挂钩子**（设计注明"不改系统提示、不改工具返回，引导只在工具说明里"），它多做的是让清单**熬过压缩**；Hermes 的周期性主动 nudge 留给**记忆蒸馏 / 技能自造**（非 todo）。对照真相是 **CC「任务清单＋壳主动软催」（周期轻提醒＋收尾无验证项补提醒，均可忽略）vs Hermes「待办工具刻意安静、硬纪律在验证门那种别处」**。过 Codex soundness+completeness（C1–C4/H1–H4 全 true；CC 侧另有 V2 Task 工具/计划验证提醒等**同向**机制，本维度措辞偏保守未夸大）。
- **验收局限（诚实记录）**：三项均 build 守卫通过、真实 `SHARED_JS` node --check OK、新增正文无英文双引号、未改任何 JS/stepper/mermaid。**契约 2 的浏览器 smoke 本次未能跑**——受控 Chrome 处于独立网络环境、无法访问本机 localhost（服务确在 127.0.0.1 监听、curl 200，但浏览器 ERR_CONNECTION_REFUSED；`file://` 被工具拒）。对纯文本改动，静态验证已覆盖其风险面；如需可由 PM 本地 `!open index.html` 目检 04 矩阵新行与 01 §04 第④维。

### 用户侧 review 整改（2026-07-02，合订本化前后）
① §04 七组 `.compare` 堆叠 → `.matrix` 矩阵组件；② 合订本离线白屏真缺陷 → `SHARED_JS` mermaid 调用加 `MERMAID_OK` 守卫；③ CSS 双份真相漂移 → build.py 从 `_design-system.html` 抽 `<style>`（单一真相）+ 叠壳 CSS，规则写进契约2；④ ch-04 死链导读改灰字；⑤ 旧口径清理（progress 结论区 + SYN"输入侧"拆为所有权/第二根杠杆、"没有防线"改"护栏只挡烧钱闯祸"）；⑥ favicon 404。**驳回**：01:272 是正确 [HM] 框架不改；full inline mermaid 暂不做。

### 早期整改（合订本化之前）
P0 脚本硬伤（03 未转义引号）已修+浏览器实测；SYN 三判词分化（控制/输出=壳杠杆·输入=模型杠杆）+ 金句改行为陈述；契约1 正文代码清扫（01/03）；四页加导航；02 流程图轻裁；01 badge/title 归位；契约2 新增"出稿必 node --check + 点一次 stepper"机械检查。

### 合订本化 & 目录整理
- **合订本架构**：源=分章（`findings/*.html`，单章可 review）；成品=`index.html`（`build.py` 装配：左栏导航、组件化 stepper 无碰撞、mermaid 按需渲染、设计系统单一真相 + 离线守卫）。
- **目录**：root = README.md / index.html / build.py + `docs/`（progress + contract + framework-and-plan + 本归档）+ `findings/`（章节源）+ `assets/`（历史截图）。文档内路径按"项目根相对"约定。

### 已敲定协议（稳定，供追溯）
HTML 自包含单文件 · 两段式流水线（结构化 JSON → 设计系统渲染）· 流程图纵向 TD + 两列等高、宽对齐正文 · 设计系统 `findings/_design-system.html` 单一真相 · 产品逻辑 not 技术细节（契约 1）。
