# 运行态看板 —— 唯一「现在该干什么」的真相源

> **首次接入三步走**：① `CLAUDE.md`（项目是什么 + 文档地图 + 铁律）→ ② **本文件**（此刻在做什么、下一步）→ ③ `backlog.csv`（能领什么、依赖是否满足）。三步到位。
> **本文件只装运行态**（在做 / 等谁 / 接下来），不装历史、结论、规则。各类信息的真相源：需求状态→`backlog.csv`；已完成结论→`archive.md`；格式与维护规则→`contract.md` 契约 4；框架/计划→`framework-and-plan.md`；成稿→`findings/`。
> **格式契约见 `contract.md` 契约 4**（区块定义 / 接棒规则 / done 即迁档 / 每活跃需求出现且仅一次）。改结构先改契约、再改本文件。
> 状态图例：🔴 在做（有 agent 推进中）· 🟡 等谁（停下等 PM）· ⚪ 接下来（未开工排队）

**📍 此刻**：主线 11 篇（轴一 3＋综合①＋轴二 5＋综合②）已成稿，结论见 `archive.md`。当前唯一在制主线＝轴一结构手术收官（**CH-A1R-04** 综合①重写，待 PM smoke 拍板）。**轴二整改 campaign `CH-A2R`**（产品观脊 ＋ 去八股 ＋ 重排）已排队：pilot＝时间章 `CH-A2R-01` 可领，其余章（含承接原 CH-05R 的用户章 `CH-A2R-05`）待 pilot 锁样板。
**环境**：CC `/Users/luohuibin/cursor_project/claude-code-main`｜Hermes `daf4f1a7a`（2026-07-01），工作区干净。
**大顺序**：整条轴一 → 综合① → 整条轴二 → 综合②（不跳轴、不并轴；无依赖者可换 session 并行）。

---

## 🔴 在做

（空 —— CH-05R 已于 2026-07-07 **撤销并入 `CH-A2R-05`**（详见「接下来」总控）；当前无 agent 主动推进：CH-A1R-04 在「等谁」候 PM smoke，CH-A2R campaign 待 pilot 领取。）

---

## 🟡 等谁

### CH-A1R-04 · 综合①重写（gating · 等 PM smoke 拍板）
- **指针**：`findings/SYN-inside-story.html`｜backlog `CH-A1R-04`｜thesis＝**八成共享 ＋ 两变量**（信任→壳厚、所有权→第二杠杆）**＋ 一条诚实例外**（产品 vs 底座）
- **恢复后第一步**：PM smoke 拍板（本地起 server 看 `findings/SYN-inside-story.html` 或合订本 `index.html#ch-syn1`）。**通过**→置 done、两处同步（backlog 状态 + 删本块）、一句话结论沉 `archive.md`（轴一收尾口径顺带升级为「八成共享+两变量+一例外」）、**CH-A1R 手术四批全闭环**；**打回**→按意见改。
- **接棒**（第 2 棒 · 2026-07-07）：新稿已并入合订本（旧版存 `assets/SYN-inside-story.v1-backup-20260707.html`）；重档 ×2＝回合时间线 stepper（8 站、⑧→② 回边吸收 PM 草图）＋分叉汇流图（8 分叉→改壳 6/改模型 1/例外 1）。机械两关 + 四道门全过（自审 4 修 / 独立完整性门 1 必修 3 可选已修 / 设计门过 / Codex soundness 按综合②先例轻量不重跑）。PM 两轮反馈已修（分叉条目全补主语 CC 蓝/Hermes 紫、药丸改竖排、术语「缝」全书改「分叉」，PM 可否决换 gap）。

---

## ⚪ 接下来（未开工排队 · 每条 = ID + 解锁条件）

1. **CH-A2R-01** 轴二整改 pilot · 时间章 — 依赖无 → **可领**（campaign 起点·锁「产品观脊+去八股」样板供其余章）
2. **CH-A2R-02 / 03 / 04 / 05** 环境 / 其他 agent / 自己 / **用户（05＝承接撤销的 CH-05R）** — 依赖 CH-A2R-01 → **pilot 定稿后可领**（四章互不依赖·可换 session 并行）
3. **EXT-01** 出错之后（切面专题）— 依赖 CH-SYN2 已 done → **可领**
4. **ART-01** Harness 产品观（独立文章，非合订本章节）— 依赖无 → **可领**；初稿 `docs/harness-product-view.md`，PM 议是否入册
5. **SYN2-R** 综合②重编 — 依赖升级为**全轴二章 done**（CH-A2R-01~05）→ 轴二整改收官后解锁
6. **MEM-09** 记忆的一生（条件项）— 依赖 CH-08 已满足，但前提被 `CH-A2R-05`「记忆整块迁 Ch05」大幅消解 → **CH-A2R-05 定稿后重估是否还写**（宁可少写不双写）

**▸ CH-A2R 总控（轴二整改 campaign · 领取前必读；框架升格待 pilot 验证）**
- **本意**：论证「harness 即产品观」——每章答一个统领问题「这个动作在替设计者解决什么用户/服务问题、合理吗」，以此替代旧「七组对照＋定点反转」模板做每章的脊。
- **新读序（按暴露产品观差异排 · 非补集序）**：时间(部署) → 用户(代码) → 自己(模型) → 环境 → 其他 agent。前三＝所有权「在哪被决定」（三个所有权对象；自己收束扣接回轴一）；后二＝所有权「后果显形」（环境=风险归属、agent=协作手段·信号最弱）。
- **每章铁律（治冗余 / 防剧透综合）**：只摊本维度机制差异＋给本维度 verdict；**不再自揭总变量**（「都是所有权/在场」这句独占留给综合②）。
- **手术刀（去八股 ≠ 去严谨）**：留＝真制品(契约3)/`file:line`/防「默认→绝对」核心 caveat/喂综合的 verdict；砍＝强制「反直觉收尾」公式、七组配额、五段铁模、每章重证总变量；caveat 墙合并降级、不独占一节。
- **绑定既有卡**：05＝`CH-A2R-05`（承接撤销的 CH-05R·结构层已就绪·渲染前套 pilot 样板）；综合②＝`SYN2-R`（吸收上述·依赖升级为全轴二章 CH-A2R-01~05）；08 结构瘦身已完成、本轮只转脊＋去八股（`CH-A2R-04`）。
- **顺序纪律**：pilot（`CH-A2R-01`）先定稿锁样板、其余章再套；**重排 build 顺序留到内容定稿后**（现改会扯断章间回指，内容重写时自然重锚）。
- **框架升格待验证**：新读序＋「产品观」仪器暂只作 campaign 工作契约；pilot 验证成立后再升进 `framework-and-plan.md`（轴二表＋契约5仪器）——不在验证前封弧（遵「防干净弧线免检」）。

---

## 🟢 决策待 PM（非阻塞，不挡任何领活）

- **CH-08 定名**：「Loop 与自己」待定名（推荐「Loop 与经验」；`CH-A2R-04` 收窄后偏「Loop 与变强/与下一代」）
- **前 9 章排版**：是否按综合②新排版标杆回头提升（PM 看完综合②再定；DSN 已落地随各批手术自然承接）
- **ART-01 入册**：Harness 产品观文章是否并入合订本

---

## 常用命令
- 重建合订本：`python3 build.py`（改章后必跑；加新章改脚本顶部 `CHAPTERS`）
- Codex 交叉核实：`codex exec -C /Users/luohuibin/cursor_project --sandbox read-only --skip-git-repo-check -o <out> "<prompt>" </dev/null`（`</dev/null` 必加）
